const submittedUserId = "485912";

const storedUserId = 485912;

const hasAccess = storedUserId === Number(submittedUserId);

// if(storedUserId === Number(submittedUserId)) {
//     hasAccess = true
// } else {
//     hasAccess = false
// }